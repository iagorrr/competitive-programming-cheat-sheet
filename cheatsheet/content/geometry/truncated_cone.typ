=== Truncated Cone

// TODO: Convert TikZ diagram to CeTZ
// Original TikZ: Truncated cone with height h, top radius r, bottom radius R, and slant height l

==== Area

$ l = sqrt(h^2 + l^2) $

$ A = pi (R+r) l + pi (R^2 + r^2) $

==== Volume

$ V = 1/3 pi h(R^2 + R r + r^2) $
