== Chicken McNugget / Postage Stamp Problem / Froenius Coin Problem

For any two *relative prime positive integers*, $m$, $n$, the greatest integer that cannot be written in the form $a m + b n$ for *nonnegative* $a$ and $b$ is $m n - m - n$.
